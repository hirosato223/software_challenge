const sql = require('./sql.js');

const setup = async () => {
  // drop friends table
  // drop users table
  // drop database
  // re-create database
  // recreate friends table
  // recreate users table
};

module.exports = setup;
